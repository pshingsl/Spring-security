spring.application.name=demo

# 서버 설정
server.address=localhost
server.port=8080

# 데이터베이스 연결
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/security?useUnicode=yes&characterEncoding=UTF-8&allowMultiQueries=true&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=1234

# JPA 설정
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto=update

#JWT
jwt.issuer=admin@spring.security.com
# 비밀키
jwt.secret_key=4291

# application.properties 영역별 설정
# src/main/resources/
# application.properties # 공통설정
# application-dev.properties # 개발 설정
# application-test.properties # 테스트 환경
# application-prod.properties # 운영환경